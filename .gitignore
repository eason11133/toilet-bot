# Secrets / local env
.env
.env.*
!.env.example
secrets.txt
secrets_backup.txt
credentials.json

# Python cache
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/
env/

# Runtime DB / logs
cache.db
*.log

# Local tools / mirrors / backups
bfg-*.jar
toilet-bot-mirror/
toilet-bot-mirror.bfg-report/
*.bak
*.tmp

# Keep production model files if intentionally committed
# Do not ignore models/*.pkl because the app loads models/clean_model.pkl and models/label_encoder.pkl

google_service_account.json
